# =============================================================================
# IRS IDMS Frontend - Environment Configuration Template
# Copy this file and modify for your target environment (GCP, T-Cloud, etc.)
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings (Used at build time via Vite)
# These are baked into the static bundle - prefix with VITE_ for client access
# -----------------------------------------------------------------------------

# Backend API URL
VITE_API_URL=https://your-backend-url.run.app

# Environment identifier
VITE_APP_ENV=production

# Application version (typically set by CI/CD)
VITE_APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# GCP/Cloud Configuration (Used by CI/CD pipeline)
# Swap these values when migrating to T-Cloud
# -----------------------------------------------------------------------------

# Project Configuration
PROJECT_ID=irsidms
REGION=us-central1

# Service Configuration
SERVICE_NAME=idms-frontend

# Container Registry
REGISTRY=us-central1-docker.pkg.dev
REPOSITORY=idms-frontend
IMAGE_NAME=idms-frontend

# Workload Identity (GCP-specific)
WORKLOAD_IDENTITY_PROVIDER=projects/602714011221/locations/global/workloadIdentityPools/github-actions-pool/providers/github-provider
SERVICE_ACCOUNT=idms-frontend-deployer@irsidms.iam.gserviceaccount.com

# -----------------------------------------------------------------------------
# Cloud Run Settings
# -----------------------------------------------------------------------------

# Resource limits
CPU=1
MEMORY=512Mi

# Scaling
MIN_INSTANCES=0
MAX_INSTANCES=10
CONCURRENCY=80

# Timeouts
REQUEST_TIMEOUT=60s
